const iconElement = document.querySelector(".weather-pic");
const iconElement = document.querySelector(".temp-degree");
const iconElement = document.querySelector(".temp-desc");
const iconElement = document.querySelector(".address");

const weather = {};

weather.temp = {
    unit : "celsius"
}